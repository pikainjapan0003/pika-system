# Step 7E-2-APP-SMOKE 合併後 App 預覽驗證

## 1. 任務背景

Step 7E Seller Agent Settings 已 merge 進 target branch，POST-MERGE-VERIFY PASS，safepoint restore dry-run SKIP RECOMMENDED（WIP 已在 target）。

本次對 merged target branch 進行 App smoke，確認服務可正常啟動並提供 `/settings/agent` 路由。

## 2. Target Branch

```
qa/step6f-cvs-store-selection-browser-mobile
HEAD: 866ce1f docs-step7e-safepoint-restore-dryrun
```

## 3. Merge / Verify Commits

| Commit    | 說明                                 |
| --------- | ------------------------------------ |
| `6611817` | merge-step7e-seller-agent-settings   |
| `f59116a` | fix-step7e-agent-settings-query-type |
| `148213c` | docs-step7e-merge-execute            |
| `d3c4154` | docs-step7e-post-merge-verify        |
| `866ce1f` | docs-step7e-safepoint-restore-dryrun |

## 4. `.replit` Stash 狀態

```
stash@{0}: On qa/step6f-pre-step7e-merge-safepoint: pre-step7e-merge-local-replit-ports
```

- **未套用**（本次任務不套 `.replit` stash）

## 5. API / Shop-app 啟動方式

### API 重新 build

dist 從 `2026-06-09 15:02` 存在，但 build 在 Step 7E merge（`6611817`）之前，故重新 build：

```bash
cd artifacts/api-server && node ./build.mjs
```

確認 dist 包含 sellerAgent（grep 36 matches）。

### API server 啟動

```bash
cd artifacts/api-server
PORT=8082 node --enable-source-maps ./dist/index.mjs &
```

Port 8080 報 EADDRINUSE（Replit 系統保留），改用 8082。

### Shop-app 啟動

```bash
cd artifacts/shop-app
PORT=5173 BASE_PATH="/" API_SERVER_PORT=8082 NODE_ENV=development vite --host 0.0.0.0 &
```

## 6. Curl Smoke 結果

| 項目                                      | HTTP 狀態                              | 結果                                   |
| ----------------------------------------- | -------------------------------------- | -------------------------------------- |
| API health `http://localhost:8082/health` | `{"error":"Not found"}` 但 server 回應 | ✓（server 運行，/health 未定義是正常） |
| Shop `/`                                  | 200                                    | ✓                                      |
| Shop `/settings`                          | 200                                    | ✓ SPA                                  |
| Shop `/settings/agent`                    | 200                                    | ✓ SPA fallback                         |
| `Settings.tsx` source 含「AI 代查設定」   | grep 命中 line 486                     | ✓                                      |
| `AgentSettings.tsx` 可存取                | Vite transform 成功                    | ✓                                      |

## 7. API Route Result

```
GET /api/stores/1/agent/settings → 401 Unauthorized
```

```json
{ "error": "Unauthorized" }
```

**結果：✓（非 404）**

- HTTP 401（x-clerk-auth-reason: dev-browser-missing）表示 route 存在，正確要求 Clerk 登入
- 未登入狀態下 401 為預期行為

## 8. DB Readonly Result

| 項目                               | 結果   |
| ---------------------------------- | ------ |
| `seller_agent_settings` table 存在 | ✓      |
| `remaining_rows` (store_id=1)      | 0 ✓    |
| DB push / migrate / seed           | 未執行 |

## 9. Manual Browser Instruction

Smoke 已通過。**請使用者在 Replit Preview 驗證：**

```
1. 打開 Replit Preview，選擇 port 5173（或 Replit 對應的 Preview URL）
2. 開啟 /settings
3. 確認頁面下方有「AI 代查設定」入口
4. 點擊後確認進入 /settings/agent
5. 確認頁面標題顯示「AI 代查設定」
6. 若要測試儲存，請在測試後清理 store_id=1 的 seller_agent_settings 資料
```

Note：

- 如果 Preview 用的是 Replit 映射 port，實際 URL 由 Replit 決定（不是 localhost:5173）
- 需要先登入（Clerk）才能看到設定頁內容

## 10. Blocking Issues

無。

## 11. Non-blocking Notes

- API server `/health` endpoint 不存在（returns `{"error":"Not found"}`），但 server 運行正常，非 Step 7E 問題
- Port 8080 / 22696 被 Replit 系統保留（EADDRINUSE），改用 8082 / 5173
- `.replit` stash 中的 port 設定（15173→8008, 19080→8000）未套用，故 Preview URL 可能與 Replit UI 顯示的 port 不同

## 12. Conclusion

```
APP-SMOKE conclusion: READY FOR USER BROWSER CHECK
```

## 13. 下一步建議

1. **使用者 browser check**：在 Replit Preview 確認 `/settings` → 「AI 代查設定」→ `/settings/agent` 頁面正常
2. **`.replit` stash 決策**：
   - 若不需要套用 → 可刪除 stash
   - 若需要套用 port 設定（15173→8008, 19080→8000）→ `git stash apply stash@{0}`，但需確認不影響其他服務
3. **Branch 清理（選做）**：
   - `qa/step6f-pre-step7e-merge-safepoint` — safepoint 保護點，可在確認後刪除
   - `qa/step7e-post-merge-safepoint-dryrun` — dry-run 分支，可刪除

---

_Generated by Claude B — Step 7E-2-APP-SMOKE — 2026-06-09_
