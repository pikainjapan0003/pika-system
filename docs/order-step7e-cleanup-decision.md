# Step 7E-2-CLEANUP-DECISION 收尾清理決策

## 1. 任務背景

Step 7E Seller Agent Settings 已完成所有子任務（MERGE-EXECUTE、POST-MERGE-VERIFY、SAFEPOINT-RESTORE-DRYRUN、APP-SMOKE、PREVIEW-EXPOSE-FIX、USER-BROWSER-CHECK-CLOSEOUT）。

本次對遺留的 `.replit` stash 和前次 session 背景 process 進行收尾清理。

## 2. Target Branch

```text
qa/step6f-cvs-store-selection-browser-mobile
HEAD: 57b908a docs-step7e-user-browser-check-closeout
```

## 3. 22696 / 8080 狀態

| 服務 | Port | PID | 狀態 |
|------|------|-----|------|
| Replit-managed shop-app | 22696 | 60319 | ✓ 200（清理後仍正常）|
| Replit-managed API | 8080 | 60495 | ✓ 401（route 存在，非 404）|

清理後重新確認：22696 和 8080 均正常運作。

## 4. `.replit` Stash 決策

**決策：不套用，直接備份後 drop**

理由：

- 此 stash 為 Step 7E merge 前的 port workaround（15173→8008, 19080→8000）
- 目前 22696 / 8080 已可正常使用，Replit Preview UI 已可選 `22696 → 3000`
- 套用舊 port 設定可能破壞目前 Replit Preview 設定

## 5. `.replit` Stash 備份與 Drop

| 項目 | 值 |
|------|-----|
| stash ref | `stash@{0}: pre-step7e-merge-local-replit-ports` |
| stash name-status | `M .replit`（只含 `.replit`，確認安全）|
| 備份路徑 | `/tmp/step7e-replit-stash-backup-20260609-175754/` |
| 備份內容 | `stash-ref.txt`, `stash-name-status.txt`, `replit-stash.patch` |
| drop 結果 | ✓ Dropped stash@{0} (1509af545faa4abf9c827f63c69a7085efcecbea) |

Drop 後剩餘 stash：

```
stash@{0}: On qa/step6f-cvs-store-selection-browser-mobile: step7e-active-preview-switch-preserve-tracked-changes
stash@{1}: On feat/step6e-buyer-cvs-api-type-security: wip-step6e-generated-types-stash
```

## 6. 8082 / 5173 Process 清理

| 項目 | PID | cwd | 結果 |
|------|-----|-----|------|
| 舊 API server（PORT=8082） | 54780 | `artifacts/api-server` | ✓ killed（PPID=1，orphaned）|
| 舊 shop-app Vite（PORT=5173） | 55863 | `artifacts/shop-app` | ✓ killed（PPID=1，orphaned）|

清理後：

- Replit-managed API（PID 60495，PORT=8080）仍保留 ✓
- Replit-managed shop（PID 60319，PORT=22696）仍保留 ✓

## 7. DB Readonly Result

| 項目 | 結果 |
|------|------|
| `seller_agent_settings` table 存在 | ✓ |
| `remaining_rows` (store_id=1) | 0 ✓ |
| DB push / migrate / seed | 未執行 |

## 8. Step 6F WIP 狀態

| 項目 | 狀態 |
|------|------|
| `artifacts/shop-app/src/pages/EditOrderDialog.tsx` | unstaged M（Step 6F CVS 門市選擇 WIP）|
| 本次是否修改 | 否（未碰）|
| 本次是否 stage | 否 |
| 本次是否 commit | 否 |

Step 6F WIP 為後續工作，不屬 Step 7E，保留原狀。

## 9. Branch Cleanup 狀態

| Branch | 本次操作 |
|--------|---------|
| `qa/step6f-pre-step7e-merge-safepoint` | 未刪（留待 final handoff 後決策）|
| `qa/step7e-post-merge-safepoint-dryrun` | 未刪（留待 final handoff 後決策）|

## 10. Blocking Issues

無。

## 11. Non-blocking Notes

- stash@{0}: `step7e-active-preview-switch-preserve-tracked-changes` 仍存在，需後續確認是否需要
- stash@{1}: `wip-step6e-generated-types-stash` 為 Step 6E，不在本次範圍

## 12. Conclusion

```
CLEANUP-DECISION conclusion: PASS — Step 7E preview cleanup complete, Step 6F WIP left untouched
```

`.replit` stash 已備份並 drop，舊背景 process（8082/5173）已清理，Replit-managed 服務（8080/22696）正常保留。

## 13. 下一步建議

1. **Step 7E 收尾完成** — 所有清理決策均已執行
2. **Step 6F 繼續**：`EditOrderDialog.tsx` CVS 門市選擇 WIP 可繼續開發與 commit
3. **stash@{0} 決策**：`step7e-active-preview-switch-preserve-tracked-changes` 是否仍需要？
4. **Branch 清理（選做）**：`qa/step6f-pre-step7e-merge-safepoint`、`qa/step7e-post-merge-safepoint-dryrun`

---

*Generated by Claude B — Step 7E-2-CLEANUP-DECISION — 2026-06-09*
