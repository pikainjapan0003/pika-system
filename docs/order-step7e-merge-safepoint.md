# Step 7E-2-MERGE-SAFEPOINT 主 workspace 合併前保護點

## 任務背景

Step 7E-2-MERGE-PREP 完成後，發現主 workspace 有多個 unstaged changes，
其中 `EditOrderDialog.tsx` 與 Step 7E source diff 重疊（HIGH RISK）。
本次任務建立 safepoint，保護既有修改，讓 target branch 回到乾淨狀態。

## 起始 Branch

```
qa/step6f-cvs-store-selection-browser-mobile
```

## Target Branch Candidate

```
qa/step6f-cvs-store-selection-browser-mobile
```

> **Target branch pending user confirmation**

## Source Branch

```
qa/step7e-seller-agent-settings-final-review
HEAD: 985424f
```

## Backup Dir

```
/tmp/step7e-merge-safepoint-20260609-152145
```

包含：
- `status.txt`：`git status --short` 快照
- `unstaged-name-status.txt`：unstaged files 名稱清單
- `unstaged.patch`：完整 diff patch（23,086 bytes）
- `replit.patch`：`.replit` 專屬 patch

> Backup 儲存在 `/tmp`，不在 git 中，重啟後可能消失。safepoint branch commit 是持久保存。

## Unstaged Changes 分類

### 起始 unstaged files

```
M .replit
M artifacts/shop-app/src/lib/printHelpers.ts
M artifacts/shop-app/src/pages/EditOrderDialog.tsx
M artifacts/shop-app/tsconfig.json
M artifacts/shop-app/vite.config.ts
```

### A. `.replit`（local port config）

- 內容：新增 port 15173→8008, 19080→8000
- 不屬於 Step 7E source diff
- 處理：**git stash**（見下方 stash ref）

### B. Step 6F Tracked Code Changes（已 commit 至 safepoint branch）

| 檔案 | 說明 |
|------|------|
| `artifacts/shop-app/src/lib/printHelpers.ts` | 印表相關修改 |
| `artifacts/shop-app/src/pages/EditOrderDialog.tsx` | 編輯訂單對話框（與 Step 7E HIGH RISK OVERLAP）|
| `artifacts/shop-app/tsconfig.json` | TypeScript 設定 |
| `artifacts/shop-app/vite.config.ts` | Vite 設定 |

### C. dev-handoff ignored files

無需處理（已在 `.gitignore`）

### D. `.claude/`

無 unstaged changes

### E. Unknown / Untracked files

無

## Safepoint Branch

```
qa/step6f-pre-step7e-merge-safepoint
```

### Safepoint Commit

```
e4b85ea wip-step6f-preserve-before-step7e-merge
```

- 包含：4 個 tracked code files
- 不包含：`.replit`、`dev-handoff/`、`.claude/`
- 4 files changed, 197 insertions(+), 181 deletions(-)

## `.replit` Stash

```
stash@{0}: On qa/step6f-pre-step7e-merge-safepoint: pre-step7e-merge-local-replit-ports
```

stash ref: `stash@{0}`
stash message: `pre-step7e-merge-local-replit-ports`

恢復方式：
```bash
git stash pop stash@{0}
```
或（在 safepoint branch 或其他 branch 上執行）：
```bash
git stash apply stash@{0}
```

## Target Branch Final Cleanliness

```
qa/step6f-cvs-store-selection-browser-mobile
```

切換回 target branch 後：
- staged changes：無
- unstaged tracked changes：無
- `.replit`：已 stash，不再 modified
- dev-handoff/：在 .gitignore，不影響

**Target branch 已達到乾淨狀態，可進行 merge。**

## 未執行項目

- 未實際 merge
- 未 git rebase / cherry-pick
- 未 DB push / migrate / seed
- 未 push GitHub
- 未 stage `.replit`
- 未 stage `dev-handoff/`
- 未 stage `.claude/`
- 未丟棄任何修改（全部保存於 safepoint branch + stash + backup patch）

## 風險與待確認

| 優先級 | 項目 |
|--------|------|
| 高 | 使用者確認 target branch（是否為 `qa/step6f-cvs-store-selection-browser-mobile`）|
| 高 | merge 後 `EditOrderDialog.tsx` 需人工合併（Step 7E + Step 6F 均有修改）|
| 中 | `/tmp` backup 在重啟後可能消失，但 safepoint branch `e4b85ea` 是持久保存 |
| 中 | `.replit` stash 在跨 branch 或重啟後仍可用，但需注意 stash@{0} 可能因其他 stash 操作移位 |
| 低 | safepoint branch 中的 `tsconfig.json` + `vite.config.ts` 是否與 Step 7E 有衝突，需 merge 後評估 |

## 下一步建議

1. **使用者確認 target branch**
2. 執行 Step 7E-2-MERGE-EXECUTE：
   ```bash
   git switch qa/step6f-cvs-store-selection-browser-mobile
   git merge qa/step7e-seller-agent-settings-final-review
   ```
3. 預期需人工解決至少 5 個高風險衝突檔案
4. Merge 完成後，從 safepoint branch cherry-pick 或手動合併 Step 6F changes：
   - `artifacts/shop-app/src/lib/printHelpers.ts`
   - `artifacts/shop-app/src/pages/EditOrderDialog.tsx`（需與 Step 7E diff 手動整合）
   - `artifacts/shop-app/tsconfig.json`
   - `artifacts/shop-app/vite.config.ts`
5. 恢復 `.replit` stash（若需要）：
   ```bash
   git stash apply stash@{0}
   ```
6. 重新 typecheck + vite build

---

*Generated by Claude B — Step 7E-2-MERGE-SAFEPOINT — 2026-06-09*
