# Step 7E-2-PREVIEW-EXPOSE-FIX Replit 可選 Preview port 修正

## 1. 任務背景

Step 7E-2-APP-SMOKE 完成後，使用者在 Replit Preview UI 找不到 port 5173。

Port 5173 為上次 Claude Code session 在背景啟動的 Vite process（PID 55863），不屬於 Replit 設定的 preview port，因此 Replit Preview UI 不顯示。

Replit Preview UI 列出的 port 22696（顯示為 `22696 → 3000`）即為 Replit 管理的 shop-app。本次任務確認 22696 上的 shop-app 是否為合併後 target branch 的版本，並提供使用者操作指示。

## 2. 5173 找不到原因

| 項目                     | 說明                                                                |
| ------------------------ | ------------------------------------------------------------------- |
| Port 5173                | 上次 Claude Code session 背景啟動（PID 55863），非 Replit 設定 port |
| Replit Preview UI        | 只顯示 Replit .replit 設定的 port                                   |
| 5173 不在 Replit Preview | Replit 未設定 5173，故 Preview UI 不暴露                            |
| 解決方式                 | 使用已列出的 22696（Replit 管理 shop-app）                          |

## 3. Target Branch

```text
qa/step6f-cvs-store-selection-browser-mobile
HEAD: 42c8670 docs-step7e-app-smoke
```

## 4. 22696 Process 調查結果

```
PID 60319 - vite --config vite.config.ts --host 0.0.0.0
  cwd: /home/runner/workspace/artifacts/shop-app
  PORT=22696, BASE_PATH=/
  API_SERVER_PORT=（未設定，預設 8080）
```

結論：22696 是 Replit 管理的 shop-app（Vite dev server），讀取磁碟上目前的 source files。

由於 target branch 已包含 Step 7E merge，source files 即為合併後版本。**不需要 kill 或重啟**。

## 5. API / Shop Service Status

| 服務                      | PID   | Port  | 說明                                          |
| ------------------------- | ----- | ----- | --------------------------------------------- |
| Replit-managed API        | 60495 | 8080  | dist 含 sellerAgent（52 matches），curl 401 ✓ |
| Replit-managed shop-app   | 60319 | 22696 | Vite dev，PORT=22696，proxy → 8080            |
| 前次 session API（背景）  | 54780 | 8082  | 仍運行，dist 含 sellerAgent ✓                 |
| 前次 session shop（背景） | 55863 | 5173  | 仍運行，非 Replit Preview port                |

## 6. Curl Smoke 結果

| 項目                                    | HTTP | 結果                               |
| --------------------------------------- | ---- | ---------------------------------- |
| `GET /settings`                         | 200  | ✓ SPA                              |
| `GET /settings/agent`                   | 200  | ✓ SPA fallback                     |
| `Settings.tsx` source 含「AI 代查設定」 | 200  | ✓ grep line 486                    |
| `AgentSettings.tsx` Vite transform      | 200  | ✓ hooks: useGetSellerAgentSettings |

## 7. API Route Result

```
GET /api/stores/1/agent/settings → 401 Unauthorized
```

```json
{ "error": "Unauthorized" }
```

**結果：✓（非 404）**

- Route 存在，Replit-managed API（port 8080）正確回傳 401
- 未登入狀態下 401 為預期行為

## 8. DB Readonly Result

| 項目                               | 結果   |
| ---------------------------------- | ------ |
| `seller_agent_settings` table 存在 | ✓      |
| `remaining_rows` (store_id=1)      | 0 ✓    |
| DB push / migrate / seed           | 未執行 |

## 9. `.replit` Stash 狀態

```
stash@{0}: On qa/step6f-pre-step7e-merge-safepoint: pre-step7e-merge-local-replit-ports
```

- **未套用**（本次任務不套 `.replit` stash）

## 10. Manual Browser Instruction

**請使用者在 Replit Preview 選擇 port 22696（顯示為 `22696 → 3000`）**：

```
1. 在 Replit Preview 面板，選取 22696 → 3000 的 URL
2. 開啟 /settings
3. 確認頁面有「AI 代查設定」入口
4. 點擊後確認進入 /settings/agent
5. 確認頁面標題顯示「AI 代查設定」
6. 若要測試儲存，請在測試後清理 store_id=1 的 seller_agent_settings 資料
```

注意：需先登入（Clerk）才能看到設定頁內容。

## 11. Blocking Issues

無。

## 12. Non-blocking Notes

- Port 5173 仍在執行（PID 55863），但不在 Replit Preview UI；不需要停止
- Port 8082 仍在執行（PID 54780，前次 API server），不影響 22696 功能
- Replit-managed API（8080）和 shop-app（22696）均已包含 Step 7E 合併後內容

## 13. Conclusion

```
PREVIEW-EXPOSE-FIX conclusion: READY — user should use Replit Preview 22696 → 3000
```

22696 上的 Replit-managed shop-app 已是合併後 target branch 的新版，API proxy 正確指向 8080（含 sellerAgent route）。

## 14. 下一步建議

1. **使用者 browser check**：在 Replit Preview 選 22696 → 3000，確認 `/settings` → 「AI 代查設定」→ `/settings/agent` 頁面正常
2. **`.replit` stash 決策**：
   - 若不需要套用 → 可刪除 stash
   - 若需要套用 port 設定（15173→8008, 19080→8000）→ `git stash apply stash@{0}`
3. **背景 process 清理（選做）**：前次 session 的 PID 54780（API 8082）和 PID 55863（shop 5173）可在確認後安全停止
4. **Branch 清理（選做）**：`qa/step6f-pre-step7e-merge-safepoint`、`qa/step7e-post-merge-safepoint-dryrun` 可在確認後刪除

---

_Generated by Claude B — Step 7E-2-PREVIEW-EXPOSE-FIX — 2026-06-09_
